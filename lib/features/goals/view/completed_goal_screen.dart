import 'package:flutter/material.dart';

class CompletedGoalScreen extends StatelessWidget {
  const CompletedGoalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('completed'),),
    );
  }
}